# 🧪 Налаштування Binance Futures Testnet

## Крок 1: Отримай Testnet API ключі

1. Відкрий **https://testnet.binancefuture.com/**
2. Натисни **"Login"** у правому верхньому куті
3. Залогінься через **GitHub** або **Google**
4. Після логіну натисни на свій email у правому верхньому куті
5. Вибери **"API Key"**
6. Натисни **"Generate HMAC_SHA256 Key"**
7. Дай назву (наприклад: "Trading Bot")
8. Скопіюй **API Key** та **Secret Key**

## Крок 2: Отримай тестові кошти

1. На сайті https://testnet.binancefuture.com/
2. У правому верхньому куті натисни на баланс
3. Натисни кнопку **"Get Test Funds"** або **"Deposit"**
4. Тобі дадуть **10,000 USDT** тестових коштів (віртуальні!)

## Крок 3: Налаштуй .env файл

Створи або відредагуй файл `.env`:

```bash
# Binance API (TESTNET)
BINANCE_TESTNET=True
BINANCE_API_KEY=твій_api_key_з_testnet
BINANCE_API_SECRET=твій_secret_key_з_testnet

# Trading Settings
SYMBOL=BTCUSDT
TIMEFRAME=4h
RISK_PER_TRADE=2.0

# Logging
LOG_LEVEL=INFO
```

⚠️ **ВАЖЛИВО:** 
- `BINANCE_TESTNET=True` - обов'язково для testnet
- Використовуй ключі з https://testnet.binancefuture.com (НЕ з binance.com!)

## Крок 4: Перебілдуй і запусти бота

```bash
# Зупини старий контейнер
docker stop liquidity-sweep-bot
docker rm liquidity-sweep-bot

# Перебілдуй
docker build -f Dockerfile.simple -t liquidity-sweep-bot .

# Запусти з новими налаштуваннями
docker run -d --name liquidity-sweep-bot \
  --env-file .env \
  -v $(pwd)/logs:/app/logs \
  liquidity-sweep-bot

# Подивися логи
docker logs -f liquidity-sweep-bot
```

## Крок 5: Перевір що все працює

Ти повинен побачити в логах:

```
✅ [TESTNET] Binance Futures Testnet initialized
✅ Account balance: $10000.00 USDT
✅ Bot initialized successfully
```

## ❓ Troubleshooting

### Помилка: "Invalid Response" або BinanceRequestException

**Причина:** Неправильні API ключі або не з Testnet

**Рішення:**
1. Переконайся що ключі з https://testnet.binancefuture.com/
2. Переконайся що `BINANCE_TESTNET=True` у .env
3. Перестворі API ключі на testnet сайті

### Помилка: APIError(code=-5000)

**Причина:** API endpoint неправильний

**Рішення:** Переконайся що використовуєш останню версію бота з виправленнями

### Баланс показує $0.00

**Причина:** Не отримав тестові кошти

**Рішення:**
1. Іди на https://testnet.binancefuture.com/
2. Натисни на баланс → "Get Test Funds"
3. Перезапусти бота

## 🎯 Коли переходити на Live?

**НЕ переходь на Live поки:**
- ❌ Не протестував бота хоча б 1-2 тижні на Testnet
- ❌ Не впевнений що стратегія працює
- ❌ Не розумієш як працює бот

**Готовий до Live коли:**
- ✅ Бот працює стабільно 1-2 тижні
- ✅ Ти розумієш всі trade які він робить
- ✅ Backtest показує прибуток
- ✅ Готовий ризикувати реальними грошима

## 🚀 Перехід на Live

Коли будеш готовий:

1. Іди на **https://www.binance.com/** → API Management
2. Створи новий API ключ
3. ✅ **ОБОВ'ЯЗКОВО** включи "Enable Futures"
4. Додай IP whitelist (IP твого сервера)
5. Зміни `.env`:
   ```bash
   BINANCE_TESTNET=False
   BINANCE_API_KEY=твій_live_api_key
   BINANCE_API_SECRET=твій_live_secret
   ```
6. Перезапусти бота

⚠️ **УВАГА:** Live торгівля = реальні гроші! Починай з малих сум!

---

## 📞 Потрібна допомога?

Якщо щось не працює:
1. Перевір логи: `docker logs liquidity-sweep-bot`
2. Перевір .env файл
3. Перевір що API ключі правильні
4. Запусти тест: `docker exec liquidity-sweep-bot python test_bot.py`

