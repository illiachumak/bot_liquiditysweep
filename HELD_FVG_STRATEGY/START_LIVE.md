# Запуск HELD FVG Bot в LIVE режимі

## Крок 1: Скопіюй .env з 4HFVG_BOT

```bash
cp ../4HFVG_BOT/.env .env
```

## Крок 2: Відредагуй .env для LIVE режиму

Відкрий `.env` і переконайся що:

```bash
# Вимкни simulation mode
SIMULATION_MODE=False

# Додай свої Binance API ключі
BINANCE_API_KEY=твій_реальний_ключ
BINANCE_API_SECRET=твій_реальний_секрет
BINANCE_TESTNET=False

# Логи
LOG_LEVEL=INFO
```

## Крок 3: Запусти бота

### Варіант A: Docker (рекомендовано для сервера)

```bash
# Використай спеціальний compose для live mode
docker compose -f docker-compose.live.yml up -d

# Подивися логи
docker compose -f docker-compose.live.yml logs -f
```

### Варіант B: Безпосередньо Python (для тестів)

```bash
python3 held_fvg_live_bot.py
```

## Перевірка конфігурації

Бот має показати при старті:

```
================================================================================
HELD FVG BOT - CONFIGURATION
================================================================================

🎯 MODE: LIVE TRADING              <-- ВАЖЛИВО! Має бути LIVE, не SIMULATION

Symbol: BTCUSDT
Initial Balance: $300.00
Risk per Trade: 2.0%

Strategy: 4h_close + rr_3.0_liq
Fixed RR: 3.0

🔴 LIVE TRADING:
  Testnet: False
  ⚠️  REAL MONEY AT RISK!
================================================================================

✅ Bot initialized in LIVE mode     <-- ВАЖЛИВО! Має бути LIVE mode
Balance: $300.00
```

Якщо бачиш "SIMULATION mode" - зупини бот і виправ `.env`!

## Troubleshooting

### Помилка: "Cannot find data files"

Це означає що `SIMULATION_MODE=True` в `.env`

**Рішення:**
```bash
# Відкрий .env
nano .env

# Зміни на:
SIMULATION_MODE=False

# Збережи і перезапусти
```

### Помилка: "API_KEY required"

**Рішення:** Додай свої API ключі в `.env`

### Помилка: "Invalid API key"

**Рішення:** Перевір що ключі правильні і мають дозволи на futures trading

## Моніторинг

```bash
# Дивись логи в реальному часі
docker compose -f docker-compose.live.yml logs -f

# Статус контейнера
docker compose -f docker-compose.live.yml ps

# Зупинка
docker compose -f docker-compose.live.yml down
```

## ВАЖЛИВІ НОТАТКИ

⚠️ **LIVE MODE = РЕАЛЬНІ ГРОШІ!**

1. Почни з малим балансом ($50-100)
2. Спостерігай перші 10 трейдів
3. Завжди використовуй стоп-лосс (2% на трейд)
4. Перевір API permissions
5. Монітор бота регулярно

---

**Ready to trade!** 🚀
