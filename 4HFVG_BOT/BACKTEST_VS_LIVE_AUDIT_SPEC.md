# SPEC: Аудит правдивості бектесту та лайв бота
## Failed 4H FVG Strategy

### Мета
Повністю перевірити правильність роботи бота і бектесту, виявити всі відмінності в логіці, які компромізують реальність результатів.

---

## 1. ЛОГІКА ВИЯВЛЕННЯ ВІДХИЛЕННЯ FVG (REJECTION)

### 1.1 Бектест (`backtest_failed_fvg.py`)

**Метод:** `update_4h_fvgs()` - рядки 255-304
- Перевіряє відхилення на **4H свічках** (рядок 280: `fvg.check_rejection(current_candle)`)
- `current_candle = df_4h.iloc[current_4h_idx]` - поточна 4H свічка
- Відхилення визначається коли:
  - BULLISH FVG: `close < bottom` після того як `entered = True`
  - BEARISH FVG: `close > top` після того як `entered = True`

**Метод:** `check_4h_rejections()` - рядки 306-320
- ⚠️ **НЕ ВИКОРИСТОВУЄТЬСЯ** в `run_backtest()`!
- Може перевіряти на 15M свічках, але не викликається

### 1.2 Лайв бот (`failed_fvg_live_bot.py`)

**Метод:** `update_4h_fvgs()` - рядки 1079-1104
- Перевіряє відхилення на **4H свічках** (рядок 1084: `fvg.check_rejection(candle)`)
- `candle` - це dict з нової 4H свічки
- Відхилення визначається так само:
  - BULLISH FVG: `close < bottom` після `entered = True`
  - BEARISH FVG: `close > top` після `entered = True`

### ✅ ВИСНОВОК 1.1
**Логіка однакова** - обидва перевіряють відхилення на 4H свічках.

---

## 2. ВІДСТЕЖЕННЯ HIGHS/LOWS ВСЕРЕДИНІ FVG

### 2.1 Бектест (`BacktestFVG.check_rejection()` - рядки 51-98)

```python
# Always track highs/lows when candle touches FVG (not just inside)
if candle_high >= self.bottom:  # Any high that reached FVG zone
    self.highs_inside.append(candle_high)
if candle_low <= self.top:  # Any low that reached FVG zone
    self.lows_inside.append(candle_low)
```

**Логіка:** Додає high/low якщо вони **досягли** FVG зони (>= bottom або <= top), навіть якщо не всередині.

### 2.2 Лайв бот (`LiveFVG.check_rejection()` - рядки 107-146)

```python
# Track highs/lows inside
if high >= self.bottom:
    self.highs_inside.append(high)
if low <= self.top:
    self.lows_inside.append(low)
```

**Логіка:** Така сама - додає high/low якщо досягли FVG зони.

### ✅ ВИСНОВОК 2.1
**Логіка однакова** - обидва відстежують highs/lows які досягли FVG зони.

---

## 3. РОЗРАХУНОК STOP LOSS

### 3.1 Бектест (`BacktestFVG.get_stop_loss()` - рядки 100-107)

```python
def get_stop_loss(self) -> Optional[float]:
    if self.type == 'BULLISH':
        if self.highs_inside:
            return max(self.highs_inside) * 1.002  # +0.2%
    else:
        if self.lows_inside:
            return min(self.lows_inside) * 0.998  # -0.2%
    return None
```

### 3.2 Лайв бот (`LiveFVG.get_stop_loss()` - рядки 148-156)

```python
def get_stop_loss(self) -> Optional[float]:
    if self.type == 'BULLISH':
        if self.highs_inside:
            return max(self.highs_inside) * 1.002  # +0.2%
    else:
        if self.lows_inside:
            return min(self.lows_inside) * 0.998  # -0.2%
    return None
```

### ✅ ВИСНОВОК 3.1
**Логіка однакова** - обидва використовують max(highs) * 1.002 або min(lows) * 0.998.

---

## 4. РОЗРАХУНОК TAKE PROFIT

### 4.1 Бектест (`create_trade()` - рядки 357-430)

**Логіка TP:**
- Якщо `use_fixed_rr = True`: `tp = entry ± (fixed_rr * risk)`
- Якщо `use_fixed_rr = False`: `tp = find_liquidity()` - шукає найближчу ліквідність

**Параметри за замовчуванням:**
- `use_fixed_rr = False` (рядок 187)
- `fixed_rr = 2.0` (рядок 187)
- Але в main (рядок 888): `use_fixed_rr = True, fixed_rr = 3`

### 4.2 Лайв бот (`create_setup_from_rejection()` - рядки 799-850)

**Логіка TP:**
- **ЗАВЖДИ** використовує `FIXED_RR = 3.0` (рядок 47)
- `tp = entry_price ± (FIXED_RR * risk)` (рядки 822-825)

### ⚠️ ВИСНОВОК 4.1
**ВІДМІННІСТЬ!** 
- Бектест може використовувати liquidity-based TP (якщо `use_fixed_rr = False`)
- Лайв бот **завжди** використовує fixed RR = 3.0
- **Проблема:** Якщо бектест запускається з `use_fixed_rr = False`, результати будуть інші!

---

## 5. ЛОГІКА EXPIRY ТА COOLDOWN

### 5.1 Бектест

**Expiry:**
- `limit_order_expiry_candles = 16` (4H на 15M timeframe = 16 свічок)
- Перевіряє fill в межах `current_15m_idx + 1` до `current_15m_idx + limit_order_expiry_candles` (рядки 649-653)

**Cooldown:**
- Після expiry встановлює `pending_setup_expiry_time = df_15m.index[cooldown_idx]` (рядок 697)
- `cooldown_idx = expiry_idx + limit_order_expiry_candles` (рядок 696)
- Тобто cooldown = 16 свічок після expiry

### 5.2 Лайв бот

**Expiry:**
- `LIMIT_EXPIRY_CANDLES = 16` (рядок 48)
- `expiry_time = datetime.now() + timedelta(hours=4)` (рядок 845)
- ⚠️ **Проблема:** 16 свічок * 15 хвилин = 240 хвилин = 4 години, але це не точно!

**Cooldown:**
- `COOLDOWN_CANDLES = 16` (рядок 49) - не використовується!
- Після expiry встановлює `pending_expiry_time = datetime.now() + timedelta(hours=4)` (рядок 934)
- Cooldown = 4 години

### ⚠️ ВИСНОВОК 5.1
**ВІДМІННІСТЬ!**
- Бектест: expiry = 16 свічок (точно), cooldown = 16 свічок (точно)
- Лайв бот: expiry = 4 години (приблизно), cooldown = 4 години (приблизно)
- **Проблема:** Час може не співпадати зі свічками через затримки API, неточність часу тощо.

---

## 6. ВАЛІДАЦІЯ SETUP (RR, SL%)

### 6.1 Бектест (`create_trade()` - рядки 401-411)

```python
# Calculate RR and SL%
reward = abs(tp - limit_price)
rr = reward / risk if risk > 0 else 0
sl_pct = risk / limit_price * 100

# Validate
if rr < self.min_rr:
    return None

if sl_pct < self.min_sl_pct:
    return None
```

**Параметри:**
- `min_rr = 2.0` (рядок 186)
- `min_sl_pct = 0.3` (рядок 186)

### 6.2 Лайв бот (`validate_setup()` - рядки 772-797)

```python
# SL distance
sl_distance_pct = abs(entry - sl) / entry * 100
if sl_distance_pct < MIN_SL_PCT:
    return False

# RR ratio
risk = abs(entry - sl)
reward = abs(tp - entry)
rr = reward / risk
if rr < FIXED_RR:  # ⚠️ ВИКОРИСТОВУЄ FIXED_RR замість min_rr!
    return False
```

**Параметри:**
- `MIN_SL_PCT = 0.3` (рядок 46)
- `FIXED_RR = 3.0` (рядок 47) - використовується для перевірки мінімального RR!

### ⚠️ ВИСНОВОК 6.1
**КРИТИЧНА ВІДМІННІСТЬ!**
- Бектест: перевіряє `rr >= min_rr` (2.0)
- Лайв бот: перевіряє `rr >= FIXED_RR` (3.0)
- **Проблема:** Лайв бот відхиляє трейди з RR < 3.0, а бектест приймає трейди з RR >= 2.0!
- Це може призвести до значно меншої кількості трейдів на лайві!

---

## 7. ЛОГІКА СТВОРЕННЯ SETUP ПІСЛЯ REJECTION

### 7.1 Бектест (`run_backtest()` - рядки 575-698)

**Умови створення setup:**
1. `not rejected_fvg.has_filled_trade` (рядок 578)
2. FVG не invalidated на поточній 15M свічці (рядок 583)
3. `current_time >= rejected_fvg.pending_setup_expiry_time` (якщо є) (рядок 590)
4. Знайдено 15M FVG правильного типу (рядки 602-607)
5. Є SL (рядок 610)
6. Є TP (рядок 616)
7. RR >= min_rr та SL% >= min_sl_pct (рядки 631-634)

**Після створення:**
- Встановлює `pending_setup_expiry_time = df_15m.index[expiry_idx]` (рядок 646)
- `expiry_idx = current_15m_idx + limit_order_expiry_candles` (рядок 645)

### 7.2 Лайв бот (`look_for_setups()` - рядки 1106-1145)

**Умови створення setup:**
1. `not self.active_trade` (рядок 1108)
2. `not self.pending_setups` (рядок 1263)
3. `can_create_setup(rejected_fvg)` (рядок 1125):
   - `not rejected_fvg.has_filled_trade` (рядок 737)
   - `datetime.now() >= rejected_fvg.pending_expiry_time` (якщо є) (рядок 742)
   - Немає активного pending setup для цього FVG (рядки 746-749)
4. FVG не invalidated (рядок 1119)
5. Знайдено 15M FVG правильного типу (рядки 1134-1135)
6. `validate_setup()` пройшов (рядок 1135)

**Після створення:**
- Встановлює `expiry_time = datetime.now() + timedelta(hours=4)` (рядок 845)
- Встановлює `pending_setup_id` (рядок 1142)

### ⚠️ ВИСНОВОК 7.1
**ВІДМІННІСТЬ!**
- Бектест: може створювати кілька setup підряд (якщо не було fill), поки не досягне cooldown
- Лайв бот: створює тільки один setup за раз (`break` на рядку 1145)
- **Проблема:** Лайв бот може пропустити можливості, якщо перший setup не заповнився!

---

## 8. ЛОГІКА FILL LIMIT ORDER

### 8.1 Бектест (`check_trade_fill()` - рядки 432-448)

```python
def check_trade_fill(self, trade: BacktestTrade, df_15m: pd.DataFrame,
                    start_idx: int, end_idx: int) -> Tuple[bool, Optional[int]]:
    for i in range(start_idx, end_idx):
        candle = df_15m.iloc[i]
        
        if trade.type == 'LONG':
            if candle['Low'] <= trade.entry_price:
                return True, i
        else:
            if candle['High'] >= trade.entry_price:
                return True, i
    
    return False, None
```

**Логіка:** Перевіряє чи low/high свічки досягли entry_price.

### 8.2 Лайв бот (`check_pending_setups()` - рядки 875-893)

```python
# Check if filled
order = self.client.get_order(SYMBOL, setup.order_id)

if order['status'] == 'FILLED':
    self.handle_setup_filled(setup, order)
```

**Логіка:** Перевіряє статус ордера через Binance API.

### ✅ ВИСНОВОК 8.1
**Логіка однакова** - обидва перевіряють чи досягнуто entry_price.

---

## 9. ЛОГІКА СИМУЛЯЦІЇ ТРЕЙДУ (EXIT)

### 9.1 Бектест (`simulate_trade()` - рядки 450-499)

```python
for i in range(entry_idx, min(entry_idx + max_candles, len(df_15m))):
    candle = df_15m.iloc[i]
    
    if trade.type == 'LONG':
        # Check SL
        if candle['Low'] <= trade.sl:
            trade.close(trade.sl, candle.name, 'SL', ...)
            break
        
        # Check TP
        if candle['High'] >= trade.tp:
            trade.close(trade.tp, candle.name, 'TP', ...)
            break
    else:  # SHORT
        # Check SL
        if candle['High'] >= trade.sl:
            trade.close(trade.sl, candle.name, 'SL', ...)
            break
        
        # Check TP
        if candle['Low'] <= trade.tp:
            trade.close(trade.tp, candle.name, 'TP', ...)
            break
```

**Логіка:** Перевіряє SL перед TP (важливо!).

### 9.2 Лайв бот (`monitor_active_trade()` - рядки 1002-1028)

```python
# Check SL order
sl_order = self.client.get_order(SYMBOL, self.active_trade.sl_order_id)
if sl_order['status'] == 'FILLED':
    self.close_trade('SL', float(sl_order['price']))
    return

# Check TP order
tp_order = self.client.get_order(SYMBOL, self.active_trade.tp_order_id)
if tp_order['status'] == 'FILLED':
    self.close_trade('TP', float(tp_order['price']))
    return
```

**Логіка:** Перевіряє SL перед TP (однаково).

### ✅ ВИСНОВОК 9.1
**Логіка однакова** - обидва перевіряють SL перед TP.

---

## 10. РОЗРАХУНОК FEES

### 10.1 Бектест (`BacktestTrade.close()` - рядки 136-160)

```python
if enable_fees:
    entry_fee = self.entry_price * self.size * maker_fee  # Entry is limit order
    if reason == 'SL':
        exit_fee = exit_price * self.size * taker_fee  # SL is market order
    else:  # TP or TIMEOUT
        exit_fee = exit_price * self.size * maker_fee  # TP is limit order
    
    total_fees = entry_fee + exit_fee
    self.pnl -= total_fees
```

**Fees:**
- `maker_fee = 0.00018` (0.18%) - рядок 201
- `taker_fee = 0.00045` (0.45%) - рядок 202

### 10.2 Лайв бот (`close_trade()` - рядки 1030-1077)

```python
# Apply fees
entry_fee = self.active_trade.entry_price * self.active_trade.size * MAKER_FEE
if reason == 'SL':
    exit_fee = exit_price * self.active_trade.size * TAKER_FEE
else:
    exit_fee = exit_price * self.active_trade.size * MAKER_FEE

pnl -= (entry_fee + exit_fee)
```

**Fees:**
- `MAKER_FEE = 0.00018` (0.18%) - рядок 52
- `TAKER_FEE = 0.00045` (0.45%) - рядок 53

### ✅ ВИСНОВОК 10.1
**Логіка однакова** - обидва використовують однакові fees.

---

## 11. РОЗРАХУНОК POSITION SIZE

### 11.1 Бектест (`create_trade()` - рядки 413-415)

```python
risk_amount = self.balance * self.risk_per_trade
size = risk_amount / risk
```

**Параметри:**
- `risk_per_trade = 0.02` (2%) - рядок 185

### 11.2 Лайв бот (`calculate_position_size()` - рядки 753-770)

```python
risk_amount = self.balance * RISK_PER_TRADE
risk_per_unit = abs(entry - sl)
size = risk_amount / risk_per_unit

# Round to lot size
size = self.client.round_to_lot_size(size)

# Check minimum notional
notional = size * entry
min_notional = 10.0
if notional < min_notional:
    size = min_notional / entry
    size = self.client.round_to_lot_size(size)
```

**Параметри:**
- `RISK_PER_TRADE = 0.02` (2%) - рядок 45

### ⚠️ ВИСНОВОК 11.1
**ВІДМІННІСТЬ!**
- Бектест: не округлює size, не перевіряє minimum notional
- Лайв бот: округлює до lot size, перевіряє minimum notional
- **Проблема:** Бектест може використовувати нереалістичні розміри позицій!

---

## 12. ОБРОБКА ACTIVE TRADE

### 12.1 Бектест

**Проблема:** `self.active_trade` ніколи не встановлюється! (рядок 208: `self.active_trade: Optional[BacktestTrade] = None`)
- Перевірка `if not self.active_trade:` завжди True (рядок 571)
- Це означає що бектест може створювати нові трейди навіть коли є активний трейд!

### 12.2 Лайв бот

**Логіка:** `self.active_trade` правильно встановлюється (рядок 997) і перевіряється (рядок 1108).

### ⚠️ ВИСНОВОК 12.1
**КРИТИЧНА ПРОБЛЕМА!**
- Бектест: може створювати кілька трейдів одночасно (нереалістично!)
- Лайв бот: тільки один трейд за раз (реалістично)
- **Проблема:** Бектест показує занадто оптимістичні результати через можливість одночасних трейдів!

---

## 13. ЛОГІКА JUMP FORWARD ПІСЛЯ CLOSE

### 13.1 Бектест (`run_backtest()` - рядки 676-688)

```python
# Jump to end of trade
if fill_idx:
    exit_time = trade.exit_time
    exit_idx = df_15m.index.get_loc(exit_time)
    current_15m_idx = exit_idx
```

**Логіка:** Після закриття трейду стрибає вперед до exit_time.

### 13.2 Лайв бот

**Логіка:** Не стрибає вперед - обробляє свічки послідовно.

### ⚠️ ВИСНОВОК 13.1
**ВІДМІННІСТЬ!**
- Бектест: пропускає свічки між entry та exit (може пропустити інші можливості!)
- Лайв бот: обробляє всі свічки послідовно
- **Проблема:** Бектест може пропустити можливості які були б доступні на лайві!

---

## 📊 ПІДСУМОК ВІДМІННОСТЕЙ

### КРИТИЧНІ ПРОБЛЕМИ:

1. **RR Validation:**
   - Бектест: `min_rr = 2.0`
   - Лайв: `FIXED_RR = 3.0` для перевірки мінімуму
   - **Вплив:** Лайв відхиляє більше трейдів

2. **Active Trade Tracking:**
   - Бектест: `active_trade` ніколи не встановлюється
   - Лайв: правильно відстежується
   - **Вплив:** Бектест може створювати кілька трейдів одночасно

3. **TP Calculation:**
   - Бектест: може використовувати liquidity-based TP
   - Лайв: завжди fixed RR = 3.0
   - **Вплив:** Різні TP = різні результати

4. **Position Size:**
   - Бектест: не округлює, не перевіряє minimum
   - Лайв: округлює та перевіряє
   - **Вплив:** Нереалістичні розміри в бектесті

5. **Expiry/Cooldown:**
   - Бектест: точні свічки (16 свічок)
   - Лайв: приблизний час (4 години)
   - **Вплив:** Можливі розбіжності в часі

6. **Jump Forward:**
   - Бектест: стрибає вперед після close
   - Лайв: послідовна обробка
   - **Вплив:** Бектест може пропустити можливості

### НЕКРИТИЧНІ ВІДМІННОСТІ:

- Setup creation logic (кількість одночасних setup)
- Cooldown implementation

---

## 🎯 РЕКОМЕНДАЦІЇ ДЛЯ ВИПРАВЛЕННЯ

1. **Виправити RR validation в лайв боті:**
   - Використовувати окремий параметр `MIN_RR` замість `FIXED_RR`

2. **Виправити active_trade tracking в бектесті:**
   - Встановлювати `self.active_trade = trade` після fill
   - Очищати після close

3. **Узгодити TP calculation:**
   - Використовувати однакову логіку (fixed RR або liquidity)

4. **Додати position size validation в бектест:**
   - Округлення до lot size
   - Перевірка minimum notional

5. **Узгодити expiry/cooldown:**
   - Використовувати точні свічки в лайв боті

6. **Виправити jump forward:**
   - Обробляти всі свічки послідовно

---

## ✅ ТЕСТОВІ СЦЕНАРІЇ

1. **Тест відхилення FVG:**
   - Створити 4H FVG, симулювати вхід та відхилення
   - Перевірити що обидва (бектест і лайв) виявляють однаково

2. **Тест створення трейду:**
   - Після відхилення 4H FVG, знайти 15M FVG
   - Перевірити що TP, SL, entry однакові

3. **Тест RR validation:**
   - Створити setup з RR = 2.5
   - Перевірити що бектест приймає, а лайв відхиляє (якщо не виправлено)

4. **Тест active trade:**
   - Створити трейд, перевірити що нові setup не створюються

5. **Тест position size:**
   - Перевірити округлення та minimum notional

6. **Тест fees:**
   - Перевірити що fees однакові для TP та SL

---

## 📝 ПРИМІТКИ

- Всі відмінності повинні бути виправлені перед використанням бектесту для прийняття рішень
- Рекомендується запустити бектест з тими ж параметрами що і лайв бот
- Перевірити результати на реальних даних

