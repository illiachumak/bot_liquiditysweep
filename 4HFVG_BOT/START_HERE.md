# 🚀 Запуск 4H FVG Bot - START HERE

## ✅ Проблема решена!

Ошибка монтирования файлов исправлена. Теперь бот готов к запуску.

## 📋 Быстрый старт (3 шага)

### 1️⃣ Настройте API ключи

```bash
cd 4HFVG_BOT

# Если .env не существует:
cp .env.example .env

# Отредактируйте .env и добавьте ваши ключи:
nano .env
```

В файле `.env` установите:
```bash
BINANCE_API_KEY=ваш_ключ
BINANCE_API_SECRET=ваш_секрет
DRY_RUN=true  # Оставьте true для тестового режима
```

### 2️⃣ Запустите бота

**Вариант A - Самый простой:**
```bash
./docker-run.sh
```

**Вариант B - Интерактивное меню:**
```bash
./docker-start.sh
```

**Вариант C - Вручную:**
```bash
mkdir -p logs
docker-compose up -d
```

### 3️⃣ Проверьте работу

```bash
# Смотрите логи в реальном времени
docker-compose logs -f

# Или проверьте статус
docker-compose ps
```

## 🎯 Основные команды

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Логи
docker-compose logs -f

# Статус
docker-compose ps
```

## 📊 Мониторинг

### Просмотр логов бота
```bash
# Из Docker
docker-compose logs -f

# Из файла (если смонтирован)
tail -f logs/live_bot.log
```

### Проверка state.json
```bash
# Посмотреть содержимое
docker-compose exec fvg-bot cat state.json | python -m json.tool

# Скопировать наружу
docker cp 4hfvg-bot:/app/state.json ./state_backup.json
```

## ⚠️ Важные замечания

### О state.json
- `state.json` находится внутри контейнера
- При перезапуске (`docker-compose restart`) - данные сохраняются ✅
- При удалении (`docker-compose down`) и пересоздании - данные теряются ❌

### Как сохранить state.json перед обновлением
```bash
# 1. Сохранить state
docker cp 4hfvg-bot:/app/state.json ./state_backup.json

# 2. Обновить и перезапустить
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# 3. Восстановить state (если нужно)
docker cp ./state_backup.json 4hfvg-bot:/app/state.json
docker-compose restart
```

## 🔒 Безопасность

Перед запуском в LIVE режиме:
- [ ] Протестирован в DRY_RUN=true режиме
- [ ] API ключ имеет только spot trading разрешения
- [ ] Выводы средств (withdrawals) ОТКЛЮЧЕНЫ
- [ ] Настроен IP whitelist (опционально)
- [ ] `.env` НЕ добавлен в git

## 📚 Дополнительная документация

| Файл | Описание |
|------|----------|
| `DOCKER_FIX.md` | Детали исправления ошибки монтирования |
| `QUICKSTART.md` | Краткое руководство по запуску |
| `DOCKER_README.md` | Полная документация по Docker |
| `LIVE_TRADING_SPEC.md` | Описание торговой стратегии |
| `TESTING_GUIDE.md` | Как тестировать бота |

## 🛠️ Troubleshooting

### Бот не запускается
```bash
# Проверьте логи
docker-compose logs

# Проверьте .env файл
cat .env

# Пересоберите образ
docker-compose build --no-cache
```

### Нет логов в ./logs
```bash
# Создайте директорию
mkdir -p logs
chmod 777 logs

# Перезапустите
docker-compose restart
```

### Забыли API ключи
```bash
# Остановите бота
docker-compose down

# Отредактируйте .env
nano .env

# Запустите снова
docker-compose up -d
```

## 🎓 Следующие шаги

1. ✅ Запустите в DRY_RUN режиме
2. ✅ Мониторьте логи несколько часов
3. ✅ Проверьте что бот правильно детектирует FVG
4. ✅ Убедитесь что ордера размещаются корректно (в testnet)
5. ⚠️ Только после успешного тестирования переходите на LIVE

## 💡 Полезные советы

### Автоматический backup state.json
Создайте cron job:
```bash
# Редактировать crontab
crontab -e

# Добавить строку (backup каждый час)
0 * * * * docker cp 4hfvg-bot:/app/state.json /backup/4hfvg_state_$(date +\%Y\%m\%d_\%H\%M).json
```

### Мониторинг ресурсов
```bash
# Использование CPU/RAM
docker stats 4hfvg-bot

# Health check
docker inspect --format='{{.State.Health.Status}}' 4hfvg-bot
```

### Просмотр всех контейнеров
```bash
docker ps -a
```

---

## 🚀 Готовы начать?

```bash
./docker-run.sh
```

**Удачной торговли! 📈**

Не забывайте: ВСЕГДА тестируйте сначала в DRY_RUN режиме!
